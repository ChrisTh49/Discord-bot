const { helpCommand } = require("./helpCommand");
const { perCommand } = require("./perCommand");
const { ytSearchCommand } = require("./youtubeSearchCommand");
const { helloCommand } = require("./helloCommand");
const { musicCommand } = require("./musicCommand");

module.exports = {
  helpCommand,
  perCommand,
  ytSearchCommand,
  helloCommand,
  musicCommand,
};
